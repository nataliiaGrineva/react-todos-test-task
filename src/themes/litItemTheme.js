import { createTheme } from '@mui/material';

export const ListItemTheme = createTheme({
  typography: {
    fontFamily: `"Grape Nuts", "Helvetica", "Arial", sans-serif`,
    fontSize: 14,
    fontWeightLight: 300,
    fontWeightRegular: 400,
    fontWeightMedium: 500
  }
});
